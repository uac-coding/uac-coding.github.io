# Week One

This first project introduces **Python** and key coding concepts: **print statements** to show output and **comments** to explain your code.

## Presentation

<GoogleSlides url="https://docs.google.com/presentation/d/e/2PACX-1vRtN9LR_CqlrH9u630xXR6z4V-vlWBcFTH6mMUX3VjvENdgwZXR4OicnSWvnWRw_A/pubembed?start=false&loop=false&delayms=0" />

## Activity

In this first project, you'll learn some of the fundamental concepts of Python programming using the Raspberry Pi online Code Editor.

### Concepts you will explore

- **Print statements**: how to display text on the screen  
- **Comments**: how to leave notes in your code for yourself and others  

Before you begin, make sure everyone is **logged into their Raspberry Pi account** and the Code Editor is open.

Open the project at: 
   https://projects.raspberrypi.org/en/projects/editor-hello-world/editor

In this project, we'll explore **basic Python programming concepts**: comments and print statements.  

Here's the full code we'll work with:

```python copy filename="hello-world.py"
# imports
# This is a comment. Comments start with '#' and are ignored by Python.
# You can use them to explain your code or leave notes for yourself.

# variables
world = '🌎🌍🌏'       # Stores globe emojis
python = 'Python 🐍'   # Stores the word Python with a snake emoji
fire = '🔥'            # Stores a fire emoji
name = 'Shirley'       # Stores a person's name

# function definitions
# Another comment placeholder for functions. Functions are reusable blocks of code.

# Put code to run under here
print(f'Hello {world}')           # Prints "Hello 🌎🌍🌏"
print(f'Welcome to {python}')     # Prints "Welcome to Python 🐍"
print(f'{python} can also do math!')  # Prints "Python 🐍 can also do math!"
print(f'{5 * 10}')                # Prints the result of 5 * 10, which is 50
```

---

<Steps>
### Comments

- Comments start with `#`.  
- Python ignores them when running your code.  
- Use comments to **explain your code**, like:

```python copy
# This is a comment
x = 5  # This comment explains the variable x
```

---

### Print Statements

`print()` shows messages in the terminal.  

- We are using **f-strings** (`f'...'`) which let you insert variables directly:  

```python copy
print(f'Hello {world}')
```

This prints:  
```
Hello 🌎🌍🌏
```

Other examples:

```python copy
print(f'Welcome to {python}')
print(f'{python} can also do math!')
print(f'{5 * 10}')
```

Outputs:

```
Welcome to Python 🐍
Python 🐍 can also do math!
50
```

- Notice how `{5 * 10}` calculates inside the f-string automatically.
</Steps>

### Next Steps

- Change the values in the code and see how the output changes.  
- Add more `print()` statements with your own emojis or math.  
- Try creating a simple message using your name and favorite emoji!
